# Configuration de déploiement Netlify — IWillDrive
# Site statique Astro, formulaire de contact géré par Netlify Forms (sans base de données).

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
